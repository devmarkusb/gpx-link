org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Optional release overrides (also supported via env in CI: ADMOB_APPLICATION_ID, ADMOB_BANNER_UNIT_ID, PLAY_REMOVE_ADS_PRODUCT_ID).
# gpxlink.admobAppId=ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY
# gpxlink.admobBannerUnitId=ca-app-pub-XXXXXXXXXXXXXXXX/ZZZZZZZZZZ
# gpxlink.removeAdsProductId=remove_ads
# Debug only: comma-separated AdMob test device id hashes (logcat prints "Use RequestConfiguration.Builder().setTestDeviceIds(Arrays.asList(\"…\"))").
# gpxlink.debugAdmobTestDeviceIds=ABCDEF012345...
